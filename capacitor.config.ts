import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.fingerpointer',
  appName: 'finger-pointer-ios',
  webDir: 'dist'
};

export default config;
